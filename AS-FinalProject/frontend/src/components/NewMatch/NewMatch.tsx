import * as React from 'react';
import {Text} from 'react-native';
import {SafeAreaView} from 'react-native-safe-area-context';

export default function NewMatch() {
  return (
    <SafeAreaView>
      <Text>New Match LIST PAGE IS WORKING</Text>
    </SafeAreaView>
  );
}
